import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "baseline",
  },

  title: {
    fontSize: 14,
    fontWeight: "bold",
    textAlign: "right",
    color: "#4EA8DE",
  },

  item: {
    fontSize: 10,
    width: 16,
    height: 16,
    marginLeft: 8,
    borderRadius: 8,
    color: "#D9D9D9",
    backgroundColor: "#333333",
    textAlign: "center",
  },
});
